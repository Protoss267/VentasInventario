nelmio_cors:
    defaults:
        allow_credentials: false
        allow_origin: ['http://localhost']

        allow_methods: ['GET', 'OPTIONS', 'POST', 'PUT', 'PATCH', 'DELETE']
        allow_headers: ['*']

        max_age: 3600

